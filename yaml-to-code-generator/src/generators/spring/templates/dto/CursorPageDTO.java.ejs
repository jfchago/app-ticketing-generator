<%# dto/CursorPageDTO.java.ejs — Generic cursor-based pagination wrapper %>
<%#
  // Only render if any entity uses cursor pagination
  const entities = Object.values(genEntities ?? {});
  const needsCursorPage = entities.some(e =>
    e.serviceMethods?.some(m => m.returnType?.startsWith('CursorPageDTO<'))
  );
  if (needsCursorPage) {
%>package <%= ir.application.basePackage %>.dto;

import lombok.AllArgsConstructor;
import lombok.Data;
import java.util.List;

@Data
@AllArgsConstructor
public class CursorPageDTO<T> {
    private List<T> items;
    private String nextCursor;
    private boolean hasMore;
}
<% } %>
